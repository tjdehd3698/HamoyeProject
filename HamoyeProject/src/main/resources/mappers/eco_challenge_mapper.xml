<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sql.hamoye.ecoChallenge_mapper">
	<insert id="insertEcoChallenge" parameterType="ecoChallenge">		
		<selectKey keyProperty="ecoChallengeId" resultType="string" order="BEFORE">
			select 'eco'||eco_seq.nextval from dual
		</selectKey>
		
		insert into 
		eco_challenge(eco_challenge_id, eco_challenge_name, prime_rate, start_date, end_date, limited_people)
		values(#{ecoChallengeId},#{ecoChallengeName},#{primeRate},#{startDate},#{endDate},#{limitedPeople})
	</insert>	
	
</mapper>









