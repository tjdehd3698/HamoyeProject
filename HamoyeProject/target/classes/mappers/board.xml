<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.ssafy.happyhouse.model.mapper.BoardMapper">
	<select id="getList" resultType="BoardDto">
	select no, title, content, writer_id
	from board
	</select>
	
	<select id="getArticle" parameterType="int" resultType="BoardDto">
	select no, title, content, writer_id
	from board
	where no=#{no}
	</select>
	
	<insert id="doWrite" parameterType="BoardDto">
	insert into board(title, content, writer_id)
	values(#{title}, #{content}, #{writer_id})
	</insert>
	
	<update id="doUpdate">
	update board
	set title=#{title}, content=#{content}
	where no=#{no}
	</update>
	
	<delete id="doDelete" parameterType="BoardDto">
	delete from board
	where no=#{no}
	</delete>
</mapper>