<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sql.hamoye.restaurant_mapper">
	<insert id="insertPoint" parameterType="point">
		<selectKey keyProperty="pointId" resultType="string" order="BEFORE">
			select 'pnt'||point_seq.nextval from dual
		</selectKey>
		
		insert into 
		point(point_id,total_point)
		values(#{pointId},#{totalPoint})
	</insert>
	
	<update id="updateTotalPoint" parameterType="hashMap">
		update point
		set total_point=total_point+TO_NUMBER(#{amount})
		where point_id = (select point_id from users where user_id = #{userId})
	</update>
	
	<delete id="deletePoint" parameterType="string">
		delete from point
		where point_id = (select point_id from users where user_id = #{value})
	</delete>
	
	<select id="getPointSum" resultType="int">
		select sum(total_point)
		from point
	</select>
	
	
</mapper>









