<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="sql.hamoye.restaurant_mapper">
	
	<select id="getRestaurantsByCategory" parameterType = "string" resultType="restaurant">
		select restaurant_id, location, restaurant_name, category, longitude, latitude
		from restaurant
		where category= #{value}
	</select>
	
	<select id="getRestaurantsByName" parameterType = "string" resultType="restaurant">
		select restaurant_id, location, restaurant_name, category, longitude, latitude
		from restaurant
		where name like '%' || #{value} || '%'
	</select>
	
	<select id="getRestaurantById" parameterType = "string" resultType="restaurant">
		select restaurant_id, location, restaurant_name, category, longitude, latitude
		from restaurant
		where restaurant_id = #{value}
	</select>
	
	<select id="getAllRestaurants" resultType="restaurant">
		select restaurant_id, location, restaurant_name, category, longitude, latitude
		from restaurant
	</select>
</mapper>









