<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sql.hamoye.account_mapper">
	<insert id="insertAccount" parameterType="account">		
		insert into 
		account(account_number, create_date, purpose, income_source, balance, prime_rate)
		values(#{accountNumber},to_date(TO_CHAR(SYSDATE,'YYYY-MM-DD'),'YYYY-MM-DD'),#{purpose},#{incomeSource},#{balance},#{primeRate})
	</insert>	
	
	<delete id="deleteAccount" parameterType="string">
		delete from account
		where account_id =(select account_id from users where user_id=#{value})
	</delete>
	
	<update id="updateBalance" parameterType="Map">
		update account
		set balance=balance+#{amount}
		where account_id = (select account_id from users where user_id =#{id})
	</update>
	
	<select id="getAllAcountCnt" resultType="account">
		select account_id
		from account
	</select>
	
	<select id="getAccountCntByDate" parameterType="string" resultType="int">
		select count(account_id)
		from account
		where to_char(create_date) = #{value}
	</select>
	
	
	
</mapper>









